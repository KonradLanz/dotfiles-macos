# AI / MCP TODO

## Current plan

- Keep MCP bootstrap tooling in `dotfiles-macos` for now.
- Add README for `mcp-filelist.sh` and the `.ai` workspace concept.
- Add `.ai` template structure for repo-local cache/state/context.
- Add checksum-based change tracking for generated and binary artifacts.
- Add a lightweight `where-did-we-stop` / `what-changed` bootstrap command.
- Evaluate a repo-local `.ai` helper before building a full MCP server.
- Build and maintain git history in parallel with `.ai` context files for every future chat.
- Add checkpoint commit guidance or helper tooling for semantic development packets.
- Extract shared pieces into generic `dotfiles` only after the workflow stabilizes across multiple repositories.

## Extraction criteria

Move from `dotfiles-macos` into generic `dotfiles` only when:
- the workflow is used across several repos,
- macOS-specific assumptions are reduced,
- the `.ai` layout and commands are stable,
- a cross-project interface is clear.
